/*const num_of_groups;
const courses = new Array(num_of_groups);
for(let i =0; i<num_of_groups; i++){
    var node = document.createElement("div");
    node.className = "group";
    courses[i] = new Array(3);
    // get the number of courses that has three/two/one star
    const num_of_courses = new Array(3);
    for(let j =0; j<3; j++){
        //num_of_courses = num of courses that has 3-j stars
        courses[i][j] = new Array(num_of_courses[j]);
        for(let k = 0; k<num_of_courses[j]; k++){
            //save the course in courses[i][j][k] 
        }
    }
}*/